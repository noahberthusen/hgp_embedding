#ifndef GENERATE_ID_H
#define GENERATE_ID_H

// BE CAREFULL: need to initialize random seed in main !!

#include "input_params.h"
#include <random>
#include <string>


std::string generate_id();

#endif
